---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: workflowruns.agents.io
spec:
  group: agents.io
  names:
    kind: WorkflowRun
    listKind: WorkflowRunList
    plural: workflowruns
    singular: workflowrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.workflowRef
      name: Workflow
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.currentStep
      name: Step
      type: integer
    - jsonPath: .status.startTime
      name: Started
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkflowRun is the Schema for the workflowruns API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkflowRunSpec defines the desired state of WorkflowRun
            properties:
              triggerData:
                description: TriggerData contains the data from the trigger event
                type: string
              workflowRef:
                description: WorkflowRef references the Workflow being run
                type: string
            required:
            - workflowRef
            type: object
          status:
            description: WorkflowRunStatus defines the observed state of WorkflowRun
            properties:
              completionTime:
                description: CompletionTime is when the run completed
                format: date-time
                type: string
              currentStep:
                description: CurrentStep is the index of the currently executing step
                type: integer
              error:
                description: Error contains error message if the run failed
                type: string
              phase:
                description: Phase is the current phase of the run
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                type: string
              startTime:
                description: StartTime is when the run started
                format: date-time
                type: string
              stepResults:
                description: StepResults contains the results of each step
                items:
                  description: StepResult contains the result of a workflow step
                  properties:
                    completionTime:
                      description: CompletionTime is when the step completed
                      format: date-time
                      type: string
                    error:
                      description: Error contains error message if the step failed
                      type: string
                    jobName:
                      description: JobName is the Kubernetes Job name for this step
                        (Pi runtime only)
                      type: string
                    name:
                      description: Name is the step name
                      type: string
                    output:
                      description: Output is the agent's response
                      type: string
                    phase:
                      description: Phase is the step status
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      - Skipped
                      type: string
                    sessionID:
                      description: SessionID is the OpenCode session ID for async
                        polling (OpenCode runtime only)
                      type: string
                    startTime:
                      description: StartTime is when the step started
                      format: date-time
                      type: string
                    tokensUsed:
                      description: TokensUsed is the total tokens consumed during
                        this step (Pi runtime only)
                      type: integer
                    toolCalls:
                      description: ToolCalls is the number of tool invocations during
                        this step (Pi runtime only)
                      type: integer
                  required:
                  - name
                  - phase
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
